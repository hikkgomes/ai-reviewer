# Framework review packs

Framework packs are compact prompts for semantic scope expansion. They identify
where to look and which negative cases to verify; they do not turn a framework
name or token into a vulnerability finding. Load the pack only when the
architecture detector or repository evidence shows that framework is present.
